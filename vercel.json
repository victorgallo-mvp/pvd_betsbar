{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @pdv/shared run build && pnpm --filter web run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
